# Zadanie-rekrutacyjne
Python version == 3.12.12
It was made using google colab so, I would advise using it too.
You need to put your API key from roboflow and adjust directories if not using google colab.
to install libraries from requirements use command:
pip install -r requirements.txt
